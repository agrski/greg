run:
  concurrency: 4
  timeout: 2m

output:
  sort-results: true

linters:
  disable-all: true
  enable:
    - bodyclose
    - errcheck
    - exhaustive
    - exportloopref
    - gci
    - goconst
    - gofmt
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - whitespace

linters-settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/agrski/greg)

issues:
  exclude-rules:
    - path: pkg/present/console/colour.go
      linters:
        - unused
